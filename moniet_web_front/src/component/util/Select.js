import "./select.css";

const Select = (props) => {
  return (
    <>
      <select>
        <option value={1}>제목</option>
        <option value={2}>작성자</option>
      </select>
    </>
  );
};

export default Select;
